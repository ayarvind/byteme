// This is a docstring for the greet function
fn greet(name: string) -> string {
    let message = `Hello, ${name}!`;
    return message;
}

let result = greet("ByteMe");
println(result);

struct User {
    name: string
    age: int
}

let u = User("Alice", 25);
println(u.name);
